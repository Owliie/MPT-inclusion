{
  "name": "mpt-inclusion",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "devDependencies": {
    "chai": "^4.3.6",
    "circomlib": "^2.0.3",
    "circomlibjs": "^0.1.2",
    "ethers": "5.6.8",
    "hardhat": "^2.9.1",
    "hardhat-circom": "^3.3.2",
    "keccak256-circom": "git+https://github.com/vocdoni/keccak256-circom"
  },
  "engines": {
    "node": ">=16"
  },
  "scripts": {
    "circom:dev": "hardhat circom --deterministic --debug --verbose",
    "circom:prod": "hardhat circom --verbose",
    "test": "hardhat test",
    "compile": "circom circuits/MPT-inclusion.circom --r1cs --wasm --sym --c"
  }
}